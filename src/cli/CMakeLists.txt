# generated by cmake-generator
cmake_minimum_required(VERSION 3.16.3)

add_executable(relay-client-transport-cli 
  main.c)


list(APPEND CMAKE_MODULE_PATH ${CMAKE_CURRENT_LIST_DIR})

include(Tornado)
set_tornado(relay-client-transport-cli)

target_include_directories(relay-client-transport-cli PUBLIC ../include)

target_link_libraries(relay-client-transport-cli 
  PUBLIC relay-client-transport
  PUBLIC udp-client)
