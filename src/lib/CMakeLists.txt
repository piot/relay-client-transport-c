# generated by cmake-generator
cmake_minimum_required(VERSION 3.16.3)

add_library(relay-client-transport STATIC 
  relay_realizer.c)


list(APPEND CMAKE_MODULE_PATH ${CMAKE_CURRENT_LIST_DIR})
include(Tornado)
set_tornado(relay-client-transport)

target_include_directories(relay-client-transport PUBLIC ../include)

target_link_libraries(relay-client-transport 
  PUBLIC relay-client
  PUBLIC guise-client)

